language: node_js

node_js:
  - 10
  - 8

addons:
  chrome: stable

install:
  - travis_retry npm install

script:
  - npm run test
  - npm run build

after_success:
  - npm run codecov

jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run semantic-release
